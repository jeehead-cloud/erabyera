# Scenario Editor UI Kit

Recreation of Atlas's level/scenario editor: tool rail, layer list, canvas, properties/inspector panel, and top toolbar. No real editor codebase was provided — layout follows the same component vocabulary as the game-session HUD.

Icons: [Lucide](https://lucide.dev) via CDN.
