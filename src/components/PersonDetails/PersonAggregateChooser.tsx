import type { PersonLocationAggregate } from '../../domain/people'
export function PersonAggregateChooser({ aggregate, onChoose, onClose }: { aggregate: PersonLocationAggregate; onChoose: (id: string) => void; onClose: () => void }) {
  return <aside className="person-details" aria-label={`People at ${aggregate.placeName ?? aggregate.placeId}`}><header className="person-details__header"><div><p className="person-details__eyebrow">People at place</p><h2>{aggregate.placeName ?? 'Referenced place unavailable'}</h2><p>{aggregate.visibleCount} represented people</p></div><button aria-label="Close people list" className="person-details__close" onClick={onClose} type="button">&times;</button></header><ul className="person-details__chooser">{aggregate.members.map((member) => <li key={member.id}><button onClick={() => onChoose(member.id)} type="button"><strong>{member.name}</strong><span>{member.roles.join(', ')} · {member.relationshipType}</span></button></li>)}</ul></aside>
}
